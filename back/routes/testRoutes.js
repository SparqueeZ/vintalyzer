const express = require("express");
const router = express.Router();
const testController = require("../controllers/authController");

// router.get("/auth", testController.auth);

module.exports = router;
